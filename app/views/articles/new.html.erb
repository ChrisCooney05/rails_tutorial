<%# rails will default to looking for a view in the directory named after the controller
with the same name as the method%>

<h1> New Article</h1>
<%#form_with is used to create forms in rails scope: :article tells the form what it is for%>
<%= form_with scope: :article, url: articles_path,
local: true do |form| %>
<%# url: gives the form a diferent action and the articles_path means rails will look at all available
url patterns for articles and will send it to the post route %>
  <% if @article.errors.any?%>
    <div id='error_explanation'>
      <h2>
        <%= pluralize(@article.errors.count, 'error') %>
  prohibited this article from being saved: 
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |err| %>
          <li><%= err %></li>
        <%end%>
      </ul>
    </div>
  <%end%>
  <%# pluralize takes a number and string, if number > 1 the string is pluralized%>
  <%# if @article.errors.any? is true we go into the if block full_messages shows the list
  we can then print them one by one%>

  <%# @article = Article.new was added to #new is to avoid @article being nil
  this would cause .errors.any to throw an error%>

  <%# rails wraps fields that have errors with field_with_errors if you want to target them%>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text%>
  <p>

  <p>
    <%= form.submit %>
  </p>
<%end%>

<%= link_to 'Back', articles_path %>